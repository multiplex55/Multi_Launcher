# Radial input recovery execution ledger

Approved specification: [radial input recovery plan](../multi_launcher_radial_input_recovery_codex_plan.md). This is one bounded implementation milestone followed by integrated verification and native acceptance.

## Start state

- `INPUT_RECOVERY_START_HEAD`: `fdb27f91beecd7e57f0d9aaebb6299282b4bbb3d` (`radial-menu-2`)
- Initial working tree: clean; no staged, unstaged, or untracked files in `git status --short --branch`.
- Immutable feature baseline remains `0d0acaf471a52f49a7ebdff61879416eefa9fc9b` (recorded in `radial-stabilization.md`).

## Milestone

| Work package | State | Evidence |
| --- | --- | --- |
| A. Designer input, focused root toggle, readiness, close | implemented; native acceptance pending | User-triggered ROOT hide sends a native visibility edge while Screen Draw and startup parking remain intact. Failed bootstrap stays read-only with Retry and Close available. The reported Designer client-input failure still needs an actual Windows trace and interaction. |
| B. Basic ring authoring, action search, Inspector handoff | implemented; native acceptance pending | The ordinary Design toolbar exposes New Menu, Add Ring, Ring and Slots; action search runs before result limiting, Inspector handoff preserves popup edits, and errors are visible. |
| C. Previewable validated geometry proposals | implemented; native acceptance pending | Candidate geometry, stale-generation protection, explicit Apply, shrink resolution and undo are implemented. Candidate preparation follows the normal service path. Apply waits for its exact prepared frame; proposal-instance tokens prevent cache reuse. |
| Integrated verification and native acceptance | tests passed; native acceptance pending | Final `cargo nextest run --no-fail-fast`: 4,635 passed, eight skipped, exit 0. Final `cargo build --bin multi_launcher`: exit 0. The actual failing Windows profile and live GUI gestures remain unobserved. |

## Final verified source and candidate

- Final test source diff SHA-256: `00461A0B51C2B1A2E709906FEBCFE02ED798797A4668E5DC3F7DCBB12997D063` relative to `INPUT_RECOVERY_START_HEAD` (the untracked ledger is excluded).
- Final Nextest: `target/input-recovery/final-nextest-verified.log` and `.meta`; 4,635 passed, eight skipped, exit 0 at 2026-09-22 21:55:38 UTC.
- Final application build: `target/input-recovery/final-build.log` and `.meta`; exit 0 at 2026-09-22 21:59:13 UTC.
- Source-matched executable: `target/debug/multi_launcher.exe`, SHA-256 `FFAB543E1F9018B9A7112B3CAAAD6132A5213BF578B841F8984251B904DCC3E7`.
- The final source passed `cargo fmt --all --check`, `cargo check --lib --bin multi_launcher`, and `git diff --check`. The test fixture-only adjustment after the last check was compiled and exercised by final Nextest.

## Native acceptance remaining

At the initial milestone commit, there was no path to the user's failing test profile or source-matched launch context. The first broken Designer client-input boundary had not been observed, and focused short-tap, Menus/Skins mouse and Tab, close, and Save/reopen could not be claimed as natively accepted. The later profile and candidate runs are recorded below; the complete Windows walkthrough remains pending.

## Follow-up native investigation (in progress)

- The user identified `G:\Repos\rust\Testing\MultiLauncher` as the test profile and authorized replacing only its executable. The original executable was preserved as `multi_launcher.exe.before-8e4e3b0b.sha32605364`; settings and radial data were not replaced.
- The first source-matched candidate (`08AF9CB74CEFE7E61CEAB061FA08D61D0FB3CE101350C6BA5396CDA756D40585`) stayed running during the user's test. The user observed that a shortcut works from another window but a focused tap does not hide the root. A hold while hidden failed to show the radial menu immediately; the following tap showed the radial menu and launcher together. This is user-observed behavior, not a traced boundary.
- A hidden-root wake bridge is implemented in the worktree. It calls `ShowWindowAsync` on the validated root HWND before egui show commands, addressing the native hidden-window redraw cycle. Independent review corrected Win32 return-value handling. `cargo check --lib --bin multi_launcher` and one full `cargo nextest run --no-fail-fast` passed after the bridge change; the full run compiled during the return-value edit and is not final source-matched verification.
- The user is also testing on another computer. This may explain why local process logs had only startup events while the user reported hotkey gestures. No local empty trace is evidence that the native hook missed a key. The opt-in acceptance trace now emits bounded typed events at warn level and includes hook entry plus a trace-ready marker. Candidate `02EB91F467FA9094EEACE2B1714D1E8CFC292D29D31BE57471CCF8F8821F2854` was built and installed in the local Testing folder. Local PID 19028 logged `trace_ready` and was stopped after the remote-test clarification; a trace from the computer receiving the gestures is pending.
- `Run-MultiLauncherTrace.ps1` is in the shared Testing folder. It launches that folder's executable with the bounded trace enabled, records its SHA-256 and PID, and writes a timestamped stderr log under `Testing\MultiLauncher\trace`.
- Remote trace `radial-20260923-000506.stderr.log` is source-matched to candidate `02EB91F467FA9094EEACE2B1714D1E8CFC292D29D31BE57471CCF8F8821F2854`. It proves physical primary-down/up and matching modifiers reached the invocation hook. Short taps at 00:05:14, 00:05:15, and 00:05:17 emitted root hide/show/hide commands; the 00:05:15 show reached an onscreen, visible native root by 00:05:15.596. The hold from 00:05:18.470 to 00:05:20.482 did not produce a visible radial surface. The next tap at 00:05:21.520 restored the root, and a radial PreviewInput pointer event was recorded at 00:05:23.214. This is the shared-folder trace from the computer receiving the shortcut, not a local simulation.
- The first broken boundary for the hold is the root GUI work queue. `RadialController::request_open` sends `PrepareRequested` to `WatchEvent::RadialPrepare`; `LauncherApp::process_watch_events` handles it only during the eframe root update. The hotkey hide path sent `ViewportCommand::Visible(false)`, starving that update until the next tap woke the HWND. The follow-up source change keeps the root drawable while parking it offscreen for a logically hidden grid, preserving GUI preparation and dispatch. Candidate `C447DC007C0C81553F118786C78E604F713AD82390C6ACD5B08C8782EF1077AC` is built and installed in Testing. `cargo check --lib --bin multi_launcher`, `cargo build --bin multi_launcher`, and `cargo fmt --all --check` passed on that candidate. Remote focused-tap and hidden-hold retest is pending.
- Remote trace `radial-20260923-001241.stderr.log` and user report establish that a hidden hold now opens an interactive radial input window without an intervening launcher tap. The user still reports that a focused short tap does not hide the main search window. The trace shows physical short taps move the root HWND to `(3000, 3000)` within milliseconds, but it did not record which window owned focus at those taps, so focused input remains unresolved. A bounded foreground-owner field was added to hook events for the next candidate.
- Remote trace `radial-20260923-001603.stderr.log` locates a Designer input boundary. The initial Snapshot reply was accepted and the Designer deferred viewport requested and consumed focus at 00:16:47.507. The generic `restore_for_new_launcher_interaction` then drove a ROOT `Focus` command at 00:16:47.538. No Designer pointer event was observed before the user struggled to close it. The restore guard only exempted opening Designer when the root had been hidden; the follow-up source change exempts Designer from root-panel restoration regardless of root visibility and tests both states. This requires a native retest; no native Designer acceptance is claimed yet.
- The controlled remote trace `radial-20260923-002444.stderr.log` repeated that Designer sequence: Snapshot accepted and Designer focus consumed at 00:25:01.899, then ROOT `Focus` at 00:25:01.934; subsequent user clicks and Tab produced no Designer pointer/widget events before the close callback. The user also reported that a focused launcher tap produced no visible change, while unfocused taps worked. This run had no `hook_primary` until after the Designer interaction, so the focused input boundary may precede the invocation service. The GUI still registered the same launcher chord with thread-local `RegisterHotKey` even when the radial low-level hook owned it; the follow-up source change prevents that duplicate registration when radial is enabled. This requires a focused native retest with foreground ownership recorded at hook entry.
